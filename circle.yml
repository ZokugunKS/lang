machine:
  node:
    version: 6

dependencies:
  pre:
    - rm -rf ./node_modules
  cache_directories:
    - ~/.npm

test:
  override:
    - npm run test-mocha
    - npm run test-karma-es5-ci
    - npm run test-karma-es6-ci
    - npm run test-karma-trident-ci